// Hotel Sircle's own social accounts, shared by the header, the mobile drawer
// and the footer so the URLs live in one place.
//
// Only the accounts that exist are listed. The remaining icons in those
// components are still placeholders — give them a value here and wire them up
// the same way once the accounts are live.
export const SOCIAL_LINKS = {
  linkedin: "https://www.linkedin.com/company/hotel-sircle/",
};

// Every one of these leaves the site.
export const externalLinkProps = {
  target: "_blank",
  rel: "noopener noreferrer",
};
